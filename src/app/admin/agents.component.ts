import { Component, inject, signal } from '@angular/core';import { CommonModule } from '@angular/common';import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';import { MockAgentService } from '../services/mock-agent.service';
@Component({standalone:true,imports:[CommonModule,ReactiveFormsModule],template:`<div class='card'><h2>Gestion agents</h2><button class='btn btn-primary' (click)='openAdd.set(true)'>Ajouter</button><table><tr *ngFor='let a of agents()'><td>{{a.nom}}</td><td>{{a.email}}</td><td>{{a.statut}}</td><td><button class='btn' (click)='toggle(a)'>Bloquer/Débloquer</button></td></tr></table><div *ngIf='openAdd()' class='card'><form [formGroup]='f' (ngSubmit)='add()'><input class='input' formControlName='nom'><input class='input' formControlName='email'><select formControlName='statut'><option value='actif'>actif</option><option value='bloque'>bloqué</option></select><button class='btn btn-primary'>Enregistrer</button></form></div></div>`})
export class AdminAgentsComponent{svc=inject(MockAgentService);fb=inject(FormBuilder);agents=signal<any[]>([]);openAdd=signal(false);f=this.fb.group({nom:['',Validators.required],email:['',[Validators.required,Validators.email]],statut:['actif',Validators.required]});ngOnInit(){this.reload()}reload(){this.svc.list().subscribe(v=>this.agents.set(v));}add(){if(this.f.invalid)return;this.svc.add(this.f.value as any).subscribe(()=>{this.openAdd.set(false);this.reload();});}toggle(a:any){this.svc.update({...a,statut:a.statut==='actif'?'bloque':'actif'}).subscribe(()=>this.reload());}}
