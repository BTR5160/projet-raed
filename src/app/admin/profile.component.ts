import { Component, inject, signal } from '@angular/core';import { CommonModule } from '@angular/common';import { FormBuilder, ReactiveFormsModule, Validators } from '@angular/forms';import { MockAuthService } from '../services/mock-auth.service';
@Component({standalone:true,imports:[CommonModule,ReactiveFormsModule],template:`<div class='card'><h2>Profil Admin</h2><p>{{auth.user()?.name}} - {{auth.user()?.email}}</p><form [formGroup]='f' (ngSubmit)='save()'><input class='input' type='password' formControlName='currentPassword' placeholder='Current'><input class='input' type='password' formControlName='newPassword' placeholder='New'><input class='input' type='password' formControlName='confirmPassword' placeholder='Confirm'><div class='error' *ngIf='f.value.newPassword!==f.value.confirmPassword'>Mot de passe non identique</div><button class='btn btn-primary' [disabled]='f.invalid||f.value.newPassword!==f.value.confirmPassword||loading()'>{{loading()?'...':'Mettre à jour'}}</button></form><div>{{msg()}}</div></div>`})
export class AdminProfileComponent{auth=inject(MockAuthService);fb=inject(FormBuilder);loading=signal(false);msg=signal('');f=this.fb.group({currentPassword:['',Validators.required],newPassword:['',[Validators.required,Validators.minLength(6)]],confirmPassword:['',Validators.required]});save(){this.loading.set(true);this.auth.updatePassword().subscribe(()=>{this.msg.set('Mot de passe mis à jour');this.loading.set(false);});}}
